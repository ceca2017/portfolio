require 'spec_helper'

describe 'Projects', type: :feature do

  before do
    visit '/'
  end


  it 'says Middleman is fun' do
    expect(page).to have_selector 'proj-title'
    within 'proj-title' do
      expect(page).to have_content 'Title: ATM Challenge'
    end
  end

end
